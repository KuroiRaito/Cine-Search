# Node
node_modules/
dist/
.env
.env.local
.vscode/
.DS_Store
# Vite
.idea/
.vite/

# eval
eval/.cache/

# supabase cli scratch
supabase/.temp/

# snapshots
# The images are for looking at locally — regenerate with `npm run snap`.
# A full sweep is >100 full-page PNGs; only the fingerprints are committed.
snapshots/**/*.png
