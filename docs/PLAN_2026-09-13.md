# Plan — harden, cut API calls, make it modular

**Approved by the owner, 2026-09-13.** Follows `docs/AUDIT_2026-09-13.md`.
Phases run in order. Each ends with a review before the next starts.
Phase D (per-module UX work) is **deferred**: the goal for now is a modular
codebase, not improved modules.

## Owner's intent, in his words

> "Think of it like SDK: login could be an SDK and I can add it wherever I
> want with some business rules and logics."

A module is therefore: a folder with a **public surface** (`index.js`),
internals nobody else imports, dependencies only on `shared/` and other
modules' public surfaces, and business rules passed in rather than baked in.

---

## Phase A — Harden *(½ day)*

The repository is public for portfolio reasons. These close the ways a
stranger could cost money or corrupt data.

| # | Item | Owner | Status |
|---|---|---|---|
| A1 | Sign-ups: block at the database with an email allowlist trigger (code), **and** disable "Enable email signups" in Supabase → Auth → Providers → Email (dashboard) | Claude / Raman | |
| A2 | Leaked-password protection: Supabase → Auth → Password → "Prevent use of leaked passwords" | Raman | |
| A3 | `/api/tmdb`: allowlist of the paths the app actually uses; same-origin `Origin`/`Referer` required in production | Claude | |
| A4 | Security response headers in `vercel.json` (`nosniff`, `Referrer-Policy`, `frame-ancestors`) | Claude | |
| A5 | Confirm the TMDB key in git history (`47d839…`) is revoked — tested live | Claude | |
| A6 | 12 commits carry a work-machine author identity. **Left as is** — rewriting public history is a force-push for no security gain. Every machine uses the GitHub noreply address from here on. | Raman | |
| A7 | 16 open Dependabot PRs, 0 security alerts. Apply as one tested update PR; close the bot PRs as superseded. | Claude | |

## Phase B — Cut TMDB calls *(1 day)*

Measured before the work (signed in, cold loads): Discover 4 · Search 6 per
query · Title 1–2 · Person 1 · **You 12** · Discover→title→back **5**.
A five-minute session is ~30 calls, nearly all re-fetching unchanged data.

| # | Item | Expected |
|---|---|---|
| B1 | Session cache in the TMDB transport: memoise GET by URL with a TTL, dedupe in-flight | every back-navigation refetch gone; −40–60 % per session |
| B2 | Cache each person's filmography totals in `catalog_people` (fill-when-absent, like season runtimes) | You 12 → 0 on repeat visits; Search's credit fetches share it |
| B3 | `/search/multi` on the unfiltered search path | Search 3 → 1 |
| B4 | Path-based edge TTLs in the proxy: titles for hours, trending for a minute | fewer TMDB-side calls across devices |
| B5 | Re-run the same measurement; record before/after in this document | numbers, not claims |

Deferred: bundling Discover's four feeds server-side (mostly a latency win).

## Phase C — Make it modular *(≈4 days, several PRs, zero behaviour change)*

The audit's work packages, plus the SDK additions:

| # | Item |
|---|---|
| C0 | Housekeeping (audit WP-0): dead files, capacitor deps, favicon, archive stale docs |
| C1 | Foundation (WP-1): `shared/`, pure `Tile`, `verify` gains **no inline styles** and **class ownership** |
| C2 | Module moves (WP-2), one PR each, **Title first**; each module gets `index.js`, `MODULE.md`, `<module>.css` |
| C3 | `verify` gains **no deep imports across modules** — only `modules/<x>/index.js` may be imported from outside `modules/<x>/` |
| C4 | Per-module snapshots at 390 / 900 / 1280, dark and light (WP-3) |

Rules for every PR in C: move, don't rewrite; screenshots identical before
and after; cut every branch from `main`.

## Phase D — Per-module UX *(deferred)*

Not now. When it starts, the per-module briefs in the audit §11 are the
starting points, one module at a time.
