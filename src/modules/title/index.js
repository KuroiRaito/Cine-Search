// Title — the public surface of this module.
//
// Everything else in this folder is internal: the screen's own components, its
// stylesheet, its breakpoints. From outside, this is the only importable path
// (`npm run verify` enforces it), so the inside can be reorganised freely.
export { default } from './Title.jsx';
